# Legacy Python Prototype

这里存放 ASnap 早期的 Python 原型代码、测试与历史 PyInstaller spec。

- `ai_screenshot/`：旧版 Python 包
- `tests/`：旧版 Python 测试
- `screenshot_ai.py`：旧入口脚本
- `pyinstaller/`：历史打包 spec

当前主线桌面实现已经迁移到仓库根目录下的 `native/`（C++20 + Qt Widgets + CMake）。
